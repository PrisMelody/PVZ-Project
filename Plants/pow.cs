using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
public class Pow : Projectile
{
    public Pow(float x, float y, Texture2D _texture)
        : base(x, y, 20, 0f, _texture)
    {
    }
}